"use client";

/** 数据源状态功能占位面板，后续 admin 分支在此展示数据源健康状态。 */
export function DataSourcePanel() {
  return (
    <section className="rounded-xl border bg-white p-4 shadow-sm">
      <div className="mb-3">
        <h2 className="text-lg font-semibold">数据源状态</h2>
      </div>
      <p className="text-sm text-muted-foreground">数据源面板占位，等待后续功能分支接入。</p>
    </section>
  );
}
